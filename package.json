{
  "name": "delivery-platform",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "seed": "tsx scripts/seed-demo-data.ts",
    "make-admin": "tsx scripts/make-platform-admin.ts",
    "typecheck:scripts": "tsc --noEmit"
  },
  "dependencies": {
    "@delivery/firebase-config": "workspace:*",
    "@delivery/shared-types": "workspace:*",
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "@delivery/config": "workspace:*",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
